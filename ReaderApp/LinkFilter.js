module.exports = LinkFilter;

function LinkFilter(title,heTitle, collectiveTitle, heCollectiveTitle, refList,category) {
  this.title = title;
  this.heTitle = heTitle;
  this.collectiveTitle = collectiveTitle;
  this.heCollectiveTitle = heCollectiveTitle;
  this.refList = refList;
  this.category = category;
}
